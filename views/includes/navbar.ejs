

    <nav class="navbar navbar-expand-lg border-bottom sticky-top">
        <div class="container-fluid">
          <a class="navbar-brand" href="/hotels"><i class="fa-solid fa-hotel"></i></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link "  href="/hotels ">Home</a>
              <%if( currUser && currUser.username == "Admin") { %>
              <a class="nav-link" href="/hotels/new">Add New hotel</a>
              <%}%>
              <%if( currUser && currUser.username == "guestAdmin") { %>
              <a class="nav-link" href="/guest/update">Update Guest</a>
              <a href="/guest" class="nav-link">Show Guest</a>
              <%}%>
           
            </div>
            <div class="navbar-nav ms-auto">
              <%if(!currUser) {%> 
              <a class="nav-link "  href="/signup ">Sign Up</a>
              <a class="nav-link" href="/login">Log in</a>
              <%}%>
              <%if(currUser) {%> 
              <a class="nav-link" href="/logout">Logout</a>
              <%}%>
              
           
            </div>
          </div>
        </div>
      </nav>
